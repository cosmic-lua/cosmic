return {
  ["id"] = "3I5xg5TLAYLYdxpSUC9jNYf1Eb4",
  ["title"] = "sandbox: one Landlock ABI probe — unveil.available() and quicksand.probe_landlock() delegate to landlock.available() instead of re-probing",
}
