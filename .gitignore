# Build outputs
o/
o2/
cosmic-first
dist/
results/

# Third-party build artifacts
3p/*/digest
3p/*/fetch

# Generated binaries (allow specific scripts). `bin/*`, not `bin/`: git
# cannot re-include a path inside an excluded DIRECTORY, so `bin/` plus
# `!bin/cosmic` silently ignores the trust root instead of tracking it.
bin/*
!bin/cosmic
!bin/cosmic.pin
!bin/make

# ah agent harness
.ah/

# Editor and IDE
.vscode/
.idea/
*.swp
*.swo
*~
.*.swp

# Embedded env vars (may contain secrets)
env.d/*
!env.d/README

# OS files
.DS_Store
Thumbs.db

# Temporary files
*.tmp
.ape*

# Test artifacts in root directory
/*.lua
!tlconfig.lua
/checkfile
/passthrough_*
/target
