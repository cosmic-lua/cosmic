/*
 * mbedtls insists on a configuration header. The configuration itself
 * is the flags in build.zig, which are part of the compile's cache key
 * where a header is not reliably; this file exists to be named.
 */
