#!/bin/sh
# Grades the notes task in an arena: see eval/check/notes.tl, which this
# runs with `cosmic --standalone` on bin/cosmic-bootstrap's cosmic: the
# pinned release, or $COSMIC_BOOTSTRAP when set.
#
#   eval/check/notes <dir>
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)
exec "$root/bin/cosmic-bootstrap" --standalone "$root/eval/check/notes.tl" "$@"
